//Esqueleto básico de um código em linguagem C
//std = standard e io = input/output
#include <stdio.h>

int main() {

    printf("Bem-vindo, quarto ano!");

    /*Se retornar zero significa que o programa não deu erro.
      Se ocorrer algum erro o programa retorna um valor diferente de zero*/
    return 0;
}
